watchCreate:
  handler: src/functions/watch.functions.create
  timeout: 30
  events:
    - http:
        method: POST
        path: watch
        cors: true
        # authorizer:
        #   name: authorizer
        #   resultTtlInSeconds: 0
